# ShadowMapping

**Source: `Engine/src/renderer/shaders/ShadowMapping.h/.cpp`**

Per-light shadow map rendering. Records a secondary [[CommandBuffer]] rendering from the light's perspective to a depth texture exposed via bindless index.
