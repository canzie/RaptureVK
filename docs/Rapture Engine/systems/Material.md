# Material

**Source: `Engine/src/assets/materials/`**

Material system with template base materials and per-entity instances. `BaseMaterial` defines a name, editable parameters, and default [[MaterialData]]. `MaterialManager` is a static singleton providing material creation and retrieval.

Instances via [[MaterialInstance]].
